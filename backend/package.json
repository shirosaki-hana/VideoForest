{
  "name": "backend",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "shirosaki-hana",
  "license": "MIT",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts",
    "typecheck": "pnpm -w exec tsc --project backend/tsconfig.json --noEmit --pretty",
    "build": "pnpm clean && pnpm run typecheck && node ./esbuild.config.mjs",
    "clean": "rimraf dist && rimraf src/generated",
    "lint": "pnpm -w exec eslint backend/",
    "lint:fix": "pnpm -w exec eslint backend/ --fix",
    "format": "pnpm exec prettier --write .",
    "format:check": "pnpm exec prettier --check ."
  },
  "dependencies": {
    "@fastify/compress": "^8.1.0",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^8.2.0",
    "@prisma/client": "^6.14.0",
    "ansi-colors": "^4.1.3",
    "axios": "^1.11.0",
    "dotenv": "^17.2.2",
    "esbuild": "^0.25.9",
    "fastify": "^5.6.0",
    "prisma": "^6.14.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "nodemon": "^3.1.10",
    "tsx": "^4.20.5"
  }
}
