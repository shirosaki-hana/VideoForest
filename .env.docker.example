# ============================================
# VideoForest Docker Environment Configuration
# ============================================
# 이 파일을 .env로 복사한 후 설정하세요:
#   cp .env.docker.example .env
#
# ⚠️  이 파일은 Docker 실행 전용입니다!
#     스탠드얼론 실행 시에는 .env.example을 참고하세요.
# ============================================

# --------------------------------------------
# 🎬 미디어 라이브러리 경로 (필수!)
# --------------------------------------------
# 호스트 시스템의 미디어 폴더 경로를 설정하세요.
# 이 경로들이 컨테이너 내부의 /media/library1, /media/library2 등으로 마운트됩니다.

# 첫 번째 미디어 라이브러리 (필수)
MEDIA_PATH_1=/path/to/your/videos

# 추가 미디어 라이브러리 (선택, docker-compose.yml에서 주석 해제 필요)
# MEDIA_PATH_2=/path/to/more/videos
# MEDIA_PATH_3=/mnt/external/movies

# --------------------------------------------
# 🌐 서버 설정
# --------------------------------------------
# 외부에서 접근할 포트 (기본: 4001)
PORT=4001

# 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=http://localhost:4001

# --------------------------------------------
# 🔐 보안 & 세션
# --------------------------------------------
# 세션 유지 시간 (예: 24h, 7d, 30d)
SESSION_TTL=24h

# Rate Limit 설정
RATELIMIT_MAX=100
RATELIMIT_WINDOWMS=10s

# --------------------------------------------
# 🎥 트랜스코딩 설정
# --------------------------------------------
# 하드웨어 인코더: Auto | NVENC | QSV | CPU
VIDEOFOREST_ENCODER=Auto

# 속도 모드 (true = 빠르지만 품질↓, false = 느리지만 품질↑)
VIDEOFOREST_SPEED_MODE=false

# --------------------------------------------
# ⚡ 프리페칭 설정
# --------------------------------------------
# 다음 세그먼트 미리 변환 (부드러운 재생)
VIDEOFOREST_PREFETCH_ENABLED=true
VIDEOFOREST_PREFETCH_COUNT=3
VIDEOFOREST_MAX_CONCURRENT_PREFETCH=2